<%= form_for @message, remote: true do |f| %>
  <%= f.label :to %>
  <%= f.collection_select :to, Contact.all, :phone, :name, include_blank: true %>
  <%= f.label :from %>
  <%= f.text_field :from %>
  <%= f.label :body %>
  <%= f.text_field :body %>
  <%= f.submit %>
<% end %>
